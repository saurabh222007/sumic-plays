# Backend environment example
PORT=5000
# Enable yt-dlp fallback (do NOT enable in production unless you understand CPU cost)
ENABLE_YTDLP=false
# Show verbose ytsr parser warnings (set true temporarily for debugging)
DEBUG_YTSR=false
# Ranking thresholds (seconds)
RANK_MIN_DURATION=30
RANK_MAX_DURATION=900
